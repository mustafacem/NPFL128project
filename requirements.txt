# Core runtime dependencies.
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
openai>=1.30.0

# openWakeWord runtime dependencies (installed explicitly so that the
# Linux-only tflite-runtime dependency, which lacks a Python 3.12 wheel,
# can be skipped in favour of the ONNX backend).
onnxruntime>=1.10.0,<2
tqdm>=4.0,<5
scipy>=1.3,<2
scikit-learn>=1,<2
requests>=2.0,<3

# Install openWakeWord itself without its dependencies:
#   pip install --no-deps openwakeword==0.6.0
openwakeword==0.6.0

# Optional: the gTTS text-to-speech backend (--tts gtts).
gTTS>=2.5.0,<3

# Optional: the offline Whisper speech recognition backend (--stt local).
# Large downloads; only needed if you do not want to use the OpenAI API.
torch>=2.2.0
transformers>=5.0.0
