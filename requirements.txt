# Core runtime dependencies.
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
openai>=2.0.0

# openWakeWord runtime dependencies (installed explicitly so that the
# Linux-only tflite-runtime dependency, which lacks a Python 3.12 wheel,
# can be skipped in favour of the ONNX backend).
onnxruntime>=1.10.0,<2
tqdm>=4.0,<5
scipy>=1.3,<2
scikit-learn>=1,<2
requests>=2.0,<3

# openWakeWord itself is deliberately NOT listed here. It pins
# tflite-runtime on Linux, which has no wheel for current Python versions, so
# listing it would make this whole file fail to install. Talker uses the ONNX
# backend instead, so install it separately without its dependencies:
#
#   pip install --no-deps openwakeword==0.6.0

# The optional backends are extras rather than requirements, so that this
# file installs only what the agent needs to run with the hosted APIs:
#
#   pip install -e ".[gtts]"    gTTS speech synthesis      (--tts gtts)
#   pip install -e ".[local]"   local Whisper model        (--stt local)
